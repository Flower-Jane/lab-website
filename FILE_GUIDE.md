# 📊 项目文件总览

## 🗂️ 完整目录结构

```
lab-website-vue/
│
├── 📄 package.json          # 项目配置文件（依赖、脚本等）
├── 📄 vite.config.js        # Vite 构建工具配置
├── 📄 index.html            # HTML 入口文件
├── 📄 .gitignore           # Git 忽略文件配置
│
├── 📄 README.md             # 详细项目说明文档
├── 📄 QUICKSTART.md         # 快速开始指南
├── 📄 start.sh              # 一键启动脚本（Linux/Mac）
│
├── 📁 src/                  # 源代码目录
│   │
│   ├── 📄 main.js          # 应用入口文件
│   ├── 📄 App.vue          # 根组件（包含导航栏和页脚）
│   │
│   ├── 📁 router/          # 路由配置
│   │   └── 📄 index.js     # 路由定义（所有页面路径）
│   │
│   ├── 📁 views/           # 页面组件（主要内容）
│   │   ├── 📄 Home.vue         # 主页（实验室简介、亮点、动态）
│   │   ├── 📄 Members.vue      # 成员介绍（导师、博士后、学生）
│   │   ├── 📄 Research.vue     # 研究方向（方向分类、合作项目）
│   │   ├── 📄 Publications.vue # 发表论文（论文列表、筛选）
│   │   └── 📄 Recruitment.vue  # 实验室招生（招生信息、联系方式）
│   │
│   └── 📁 assets/          # 静态资源
│       └── 📄 style.css    # 全局样式文件
│
└── 📁 node_modules/        # npm 依赖包（npm install 后生成）
└── 📁 dist/                # 构建输出目录（npm run build 后生成）
```

---

## 📄 核心文件说明

### 配置文件

| 文件 | 作用 | 是否需要修改 |
|------|------|-------------|
| `package.json` | 定义项目依赖和脚本命令 | ⚠️ 一般不需要 |
| `vite.config.js` | Vite构建工具配置 | ⚠️ 需要改端口时修改 |
| `index.html` | HTML入口 | ❌ 不需要 |

### 应用核心

| 文件 | 作用 | 是否需要修改 |
|------|------|-------------|
| `src/main.js` | 应用启动入口 | ❌ 不需要 |
| `src/App.vue` | 根组件（导航栏、页脚） | ⚠️ 修改导航栏或页脚时 |
| `src/router/index.js` | 路由配置 | ⚠️ 添加新页面时 |

### 页面组件（重点修改）

| 文件 | 内容 | 修改频率 |
|------|------|---------|
| `src/views/Home.vue` | 主页内容 | ⭐ 经常 |
| `src/views/Members.vue` | 成员信息 | ⭐⭐⭐ 非常频繁 |
| `src/views/Research.vue` | 研究方向和项目 | ⭐⭐ 频繁 |
| `src/views/Publications.vue` | 论文列表 | ⭐⭐⭐ 非常频繁 |
| `src/views/Recruitment.vue` | 招生信息 | ⭐ 经常 |

### 样式文件

| 文件 | 作用 | 是否需要修改 |
|------|------|-------------|
| `src/assets/style.css` | 全局样式（颜色、字体、布局） | ⚠️ 需要改外观时 |

---

## 🎯 常见修改任务

### 任务1: 添加新成员

**文件:** `src/views/Members.vue`

**位置:** 在 `<script setup>` 中找到对应数组

```javascript
const phdStudents = ref([
  // 在这里添加新成员
  { id: 4, name: '新同学', avatar: '👨', year: '2024', research: '研究方向' }
])
```

---

### 任务2: 添加新的研究方向

**文件:** `src/views/Research.vue`

**位置:** `researchDirections` 数组

```javascript
const researchDirections = ref([
  // 添加新方向
  {
    icon: '🔬',
    title: '新研究方向',
    description: '描述...',
    keywords: ['关键词1', '关键词2'],
    projects: [
      // 项目列表
    ]
  }
])
```

---

### 任务3: 在研究方向下添加合作项目

**文件:** `src/views/Research.vue`

**位置:** 在某个研究方向的 `projects` 数组中

```javascript
projects: [
  {
    id: 1,
    name: '项目名称',
    partner: '合作机构',
    duration: '2024-01 至 2026-01',
    description: '项目描述',
    link: 'https://项目链接',
    paper: 'https://论文链接'
  }
]
```

---

### 任务4: 添加新论文

**文件:** `src/views/Publications.vue`

**位置:** `publications` 数组

```javascript
const publications = ref([
  {
    id: 1,
    title: '论文标题',
    authors: '作者1, 作者2',
    venue: '发表期刊/会议',
    year: 2024,
    type: 'journal', // 或 'conference'
    isTop: true,     // 是否顶级
    citations: 100,
    pdf: 'PDF链接',
    link: '论文链接'
  }
])
```

---

### 任务5: 修改网站主题色

**文件:** `src/assets/style.css`

**搜索:** `#667eea` 和 `#764ba2`

**替换为:** 你喜欢的颜色

```css
/* 原来的渐变色 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 改成你的颜色 */
background: linear-gradient(135deg, #你的颜色1 0%, #你的颜色2 100%);
```

---

### 任务6: 修改联系方式

**文件:** `src/App.vue`（页脚）和 `src/views/Recruitment.vue`（招生页）

**搜索:** `lab@university.edu` 等

---

## 💡 文件修改小技巧

### 使用VS Code搜索功能

1. 按 `Ctrl+Shift+F`（Windows）或 `Cmd+Shift+F`（Mac）
2. 输入要搜索的内容
3. 可以直接在搜索结果中修改

### 查找数据位置

每个页面组件的数据都在 `<script setup>` 部分：

```vue
<template>
  <!-- 这里是HTML -->
</template>

<script setup>
// ⭐ 数据都在这里！
import { ref } from 'vue'

const myData = ref([
  // 修改这里的数据
])
</script>

<style scoped>
/* 这里是样式 */
</style>
```

---

## 🔍 快速定位代码

| 要修改的内容 | 搜索关键词 | 文件 |
|------------|----------|------|
| 导师信息 | `const advisors` | Members.vue |
| 博士生信息 | `const phdStudents` | Members.vue |
| 研究方向 | `const researchDirections` | Research.vue |
| 论文列表 | `const publications` | Publications.vue |
| 招生类型 | `const recruitmentTypes` | Recruitment.vue |
| 主题颜色 | `#667eea` | style.css |
| 联系邮箱 | `lab@university.edu` | App.vue, Recruitment.vue |

---

## 📋 文件修改优先级

**高频修改（⭐⭐⭐）:**
- `src/views/Members.vue` - 成员变动
- `src/views/Publications.vue` - 发表新论文

**中频修改（⭐⭐）:**
- `src/views/Research.vue` - 新项目
- `src/views/Home.vue` - 更新动态

**低频修改（⭐）:**
- `src/views/Recruitment.vue` - 招生信息调整
- `src/assets/style.css` - 样式调整
- `src/App.vue` - 导航栏调整

**几乎不改:**
- `src/main.js`
- `src/router/index.js`
- `vite.config.js`
- `package.json`

---

**需要帮助？** 查看 `QUICKSTART.md` 或 `README.md` 获取更多信息！
